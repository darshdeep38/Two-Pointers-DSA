The Two Pointer Technique is a very popular problem-solving strategy in Data Structures & Algorithms.
It helps you optimize time complexity, usually from O(n²) → O(n), by using two indices (pointers) instead of nested loops.
